P18-ETAT - Diagnostic perte de calibration/perspective (Volet 1 seul, lecture seule)
CONSTAT PREALABLE: git diff --name-only b76b60c..HEAD filtre camera|perspective|scene|studio|placement|calibration = VIDE. Aucun fichier camera/perspective modifie sur les 3 dernieres passes. Aucun rollback execute, rien restaure, rien devine.
HYPOTHESE 1 INFIRMEE: grep localStorage|sessionStorage|indexedDB|deleteDatabase|.clear() sur web/index.html ne retourne AUCUNE ligne de purge (seules lignes presentes: serviceWorker.unregister() et caches.delete(), Cache Storage API, distinct de localStorage). Rien retire, aucune ligne ne correspondait au motif recherche.
TEST EMPIRIQUE (Playwright headless, URL publique): sonde manuelle localStorage.setItem('__test_probe__', ...) posee avant reload, relue IDENTIQUE apres F5. localStorage n'est PAS purge par ce chargement de page. Cle 'sds_state' absente avant ET apres (jamais ecrite car aucune action utilisateur simulee dans ce test, coherent avec save() qui n'est appele que sur mutation d'etat).
PERSPCALIB/ISCALIBRATED: confirme exclus de save()/restore() par conception (app_state.dart lignes ~903-950, commentaire documente: ancienne calibration aberrante rejouee au-dessus d'une calibration fraiche fiable). Comportement NON modifie, toute recharge de page perd la calibration - c'est voulu.
OVERLAY HORS RECT IMAGE (signale P18-ETAT, CORRIGE par P18-HOTFIX, commit 15a0ee5): room_painter.dart:paint() enveloppe desormais le dessin des overlays (canvas.save()+canvas.clipRect(dst) avant, canvas.restore() dans un finally apres, dst = exactement le Rect drawImageRect/imgDraw) via une extraction dans _paintOverlays() - clip actif uniquement quand une vraie photo est chargee (dst != null), jamais sur _paintDemoRoom. VERIFICATION GEOMETRIQUE POST-FIX (calcul hors-Flutter, preset 'haussmann', reproduisant exactement CalibCanvasPoints.fromCalib + cornice_plinth_painter.dart): sur un canvas CARRE (letterbox haut/bas ~212px) avec la famille Corniches/D720, AUCUN point de la bande ne sort du dst - le clip n'a rien a couper dans cette configuration precise, donc rien a demontrer visuellement avec ce couple canvas/famille. En revanche sur un canvas PLUS LARGE que la photo (letterbox gauche/droite, ex. 2000x1000 -> dst_left=282.11), le point d'onglet lateral de la PLINTHE (wallBLt, cote gauche) calcule a x=269.4 - soit ~13px AU-DELA de dst_left avant clip - confirmant que le bug reel existe et que le clip du hotfix le supprime bien dans cette configuration. Capture avant/apres visuelle avec ce couple (canvas pillarbox + famille Plinthes) non encore produite a ce jour - reste a faire dans une passe dediee si une preuve image est requise.
RESULTATS HTTP (avant/apres, aucun changement deploye): /=200, main.dart.js=200, assets/profiles/index.json=200 avec 43 refs. PID serveur 8080 inchange (117698).
DOWNTIME: 0 seconde - aucun fichier modifie dans ce volet, donc aucun rebuild/swap/redemarrage necessaire ni execute.
CONCLUSION VOLET 1 (inchangee): aucune correction de code appliquee dans ce volet (rien a corriger dans web/index.html, comportement perspCalib/isCalibrated conforme a la conception).
MISE A JOUR POST P18-HOTFIX (commit 15a0ee5, ajoutee a posteriori, ne remplace pas le constat Volet 1 ci-dessus): le clipRect manquant signale ci-dessus est desormais corrige dans room_painter.dart. flutter analyze=108 issues (baseline), flutter test=283 passed/1 skipped, tous verts, aucune regression detectee. Deuxieme correctif bundle dans le meme commit: motif_preview.dart bascule Image.network vers Image.asset local (assets/profiles/control/<ref>.png) avec errorBuilder, supprimant la dependance reseau CORS des vignettes "Relief reel" (12/43 refs sans PNG de controle affichent un placeholder neutre, comportement attendu, non un bug).
